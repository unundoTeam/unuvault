{
  "name": "@unuvault/browser-extension",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "test": "vitest --run tests",
    "lint": "tsc --noEmit -p tsconfig.json"
  }
}
